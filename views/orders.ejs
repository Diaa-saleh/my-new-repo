
   <%- include('parts/header')%>
   <%- include('parts/nav')%>

      <table>
         <thead>
            
         </thead>
         <tbody>
            <% for(let i = 0; i < orders.length; i++) { %>
               <tr>
                  <td><%= i + 1 %></td>
                  <td><%= orders[i].product %></td>
                  <td><input type="text" name="amount" value="<%= orders[i].amount %>"></td>
                  <td><%= orders[i].price * orders[i].amount %></td>
                  <td><%= orders[i].address %></td>
                  <td><%= orders[i].status%></td>
                  <td><%= orders[i].timeStamp%></td>
                     
                  <form method="POST">  
                     <td> 
                        <input type = "hidden" name="product_Id" value="<%= orders[i].product_Id%>">
                        <input type="submit" class="btn btn-danger" value="cancel" formaction="/orders/cancel">
                     </td>
                  </form>
               </tr>
               <% } %>
            </tbody>
            
         
      </table>
      <div class="container deleteall-orderall">
            <form method="POST">
               <input type="submit" class="btn btn-danger" value="cancelAll" formaction="/orders/cancelAll">
            </form>
         </div>
   
   <%- include('parts/footer')%>
